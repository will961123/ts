import onePic from "./one-pic.vue";
import threePic from "./three-pic.vue";
import noPic from "./no-pic.vue";

const getAllComponents = () => {
  return { onePic, threePic, noPic };
};
export { getAllComponents };
